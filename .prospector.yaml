ignore-paths:
  - doc
  - build
  - versioneer.py
  - metpy/_version.py
# Until the SyntaxError this causes with PyLint is fixed
  - metpy/io/nexrad.py

pylint:
  disable:
    - unbalanced-tuple-unpacking
pep8:
  options:
    max-line-length: 95
